 insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907000814' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907003269' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907005558' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908010094' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908010098' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908010657' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908010445' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908010444' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908008392' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907007835' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907007833' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907005080' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907003278' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907003265' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907003111' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907000762' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908012102' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908013397' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908013202' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908013063' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908012755' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908011609' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908009353' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908008415' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907006340' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907006241' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907004708' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907004412' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907004408' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907003505' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907002138' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908011855' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908011849' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908011848' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907005087' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907004914' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907004782' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907000652' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907006191' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907006188' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211909021295' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211909021292' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907004492' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908012107' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908011913' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908011884' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211909021073' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211909021068' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211909016595' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211909016592' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211908010416' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907007917' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907007780' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907007777' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211909021295' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211909021292' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211909023390' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211909023389' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211909023387' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   'Refund Premi',1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='211907002064' group by b.policyno,a.regid) bb  insert into tr_billing (billno,billdt,duedt,policyno,reffno,grossamt,nettamt,admamt,discamt,totalamt,remark,billtype) select concat(aa.prevno,DATE_FORMAT(now(),aa.formdt),aa.billno) sbillno,  now(),date_add(now(),interval 15 day),bb.policyno,regid endorsno,gpremi,gpremi,0,0,gpremi,   concat('Refund Premi ; tanggal batal ',bb.tglbatal)  ,1   from (select a.prevno, concat(right(concat(a.formseqno,b.lastno),formseqlen)) billno ,formdt   from tbl_lastno_form a inner join tbl_lastno_trans b on a.trxid=b.trxid  where a.trxid='billpre') aa ,   (select a.regid,b.policyno,sum(a.refund)*-1 gpremi,a.tglbatal from tr_sppa_cancel a   inner join tr_sppa b on a.regid=b.regid   where a.regid='' group by b.policyno,a.regid) bb INSERT INTO tr_billing
                                (billno,
                                 billdt,
                                 duedt,
                                 policyno,
                                 reffno,
                                 grossamt,
                                 nettamt,
                                 admamt,
                                 discamt,
                                 totalamt,
                                 remark,
                                 billtype)
                    SELECT Concat(aa.prevno, Date_format(Now(), aa.formdt), aa.billno) sbillno,
                           Now(),
                           Date_add(Now(), interval 15 day),
                           bb.policyno,
                           regid                                                       endorsno,
                           gpremi,
                           gpremi,
                           0,
                           0,
                           gpremi,
                           Concat('Refund Premi ; tanggal batal ', bb.tglbatal),
                           1
                    FROM   (SELECT a.prevno,
                                   Concat(Right(Concat(a.formseqno, b.lastno), formseqlen)) billno,
                                   formdt
                            FROM   tbl_lastno_form a
                                   inner join tbl_lastno_trans b
                                           ON a.trxid = b.trxid
                            WHERE  a.trxid = 'billpre') aa,
                           (SELECT a.regid,
                                   b.policyno,
                                   SUM(a.refund) *- 1 gpremi,
                                   a.tglbatal
                            FROM   tr_sppa_cancel a
                                   inner join tr_sppa b
                                           ON a.regid = b.regid
                            WHERE  a.regid = '212003055582'
                            GROUP  BY b.policyno,
                                      a.regid) bb  INSERT INTO tr_billing
                                (billno,
                                 billdt,
                                 duedt,
                                 policyno,
                                 reffno,
                                 grossamt,
                                 nettamt,
                                 admamt,
                                 discamt,
                                 totalamt,
                                 remark,
                                 billtype)
                    SELECT Concat(aa.prevno, Date_format(Now(), aa.formdt), aa.billno) sbillno,
                           Now(),
                           Date_add(Now(), interval 15 day),
                           bb.policyno,
                           regid                                                       endorsno,
                           gpremi,
                           gpremi,
                           0,
                           0,
                           gpremi,
                           Concat('Refund Premi ; tanggal batal ', bb.tglbatal),
                           1
                    FROM   (SELECT a.prevno,
                                   Concat(Right(Concat(a.formseqno, b.lastno), formseqlen)) billno,
                                   formdt
                            FROM   tbl_lastno_form a
                                   inner join tbl_lastno_trans b
                                           ON a.trxid = b.trxid
                            WHERE  a.trxid = 'billpre') aa,
                           (SELECT a.regid,
                                   b.policyno,
                                   SUM(a.refund) *- 1 gpremi,
                                   a.tglbatal
                            FROM   tr_sppa_cancel a
                                   inner join tr_sppa b
                                           ON a.regid = b.regid
                            WHERE  a.regid = '211907000620'
                            GROUP  BY b.policyno,
                                      a.regid) bb  